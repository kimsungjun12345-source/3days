# 셋돌하나 — 작업 안내

3일짜리 습관 앱. 웹 코드 한 벌(`index.html` + `app.js` + `style.css`)로
웹·PWA·iOS·안드로이드를 모두 낸다.

앱 이름은 **셋돌하나**다(패키지 `com.trevicode.setdolhana`). '작심삼일'은
이제 브랜드명이 아니라 이 앱이 뒤집어 쓰는 **관용구/개념**으로만 남는다 —
태그라인 "작심삼일도 여러 번 하면 평생이 된다"처럼. 브랜드로 쓰이던
'작심삼일'은 전부 '셋돌하나'로 바꿨고, 관용구는 그대로 둔다. 새 카피에서
'완주' 같은 기능명은 '3일 완주'로 적어 브랜드와 섞이지 않게 한다.

제품 설명은 `README.md`, 앱 빌드와 스토어 출시는 `BUILD.md`에 있다.
무슨 앱인지는 `docs/POSITIONING.md`, 돌·워드마크·아이콘의 치수는
`docs/BRAND.md`, 측정 지표는 `docs/ANALYTICS.md`에 있다.
이 문서는 **코드를 고칠 때 알아야 할 것**만 적는다.

## 무엇보다 먼저

이 앱의 전제는 **"무너져도 실패가 아니다"**이다. 연속 기록을 0으로
되돌리거나, 건너뛴 날을 빨갛게 칠하거나, 사용자를 다그치는 문구를
넣는 변경은 이 앱의 제품 자체와 어긋난다. 숫자로 세는 것은 '며칠
연속'이 아니라 **쌓은 날(`totalDays`)과 다시 온 횟수(`restarts`)**다.

## 구조

빌드 도구도 프레임워크도 없다. 파일을 고치면 그게 곧 앱이다.
`index.html`을 브라우저로 열면 바로 뜬다.

```
index.html    화면 뼈대 (모든 화면이 여기 다 들어 있고 hidden으로 여닫는다)
app.js        본체 2,500줄 — 아래 섹션 구조를 따른다
style.css     전부. CSS 변수로 밝은/어두운 테마를 함께 정의한다
icons.js      SVG 아이콘
native.js     앱으로 감쌌을 때만 켜지는 다리 (햅틱·알림·공유)
share.js      공유 카드 그리기
sw.js/pwa.js  서비스 워커와 설치
```

`app.js`는 `/* ── 제목 ── */` 주석으로 나뉜다. 고칠 곳을 찾을 때는
이 표지를 먼저 훑는 게 빠르다: 지금 몇 시인가 · 저장소 · 상태 판정 ·
액션 · 돌탑 SVG · 돌탑 정원 · 작심마다 다른 돌 · 렌더링 · 달력 ·
기록 탭 · 정원 탭 · 화면 전환 · 완주 축하 · 처음 만나는
안내 · 바텀시트 · 개발자 도구 · 인트로

## 명령

```bash
npm test            # 스모크 → PWA → 명암비, 셋 다 (커밋 전 필수)
node test/smoke.js  # 앱 흐름 하나만 빠르게
npm run build       # www/ 만들기 (네이티브 동기화 전 단계)
```

테스트는 실제 Chromium을 띄운다. 실행 파일을 찾는 순서는 `test/browser.js`
한 곳에 있다 — `CHROMIUM_PATH` → 브라우저 보관함 → playwright 기본값.
없는 환경에서는 `npx playwright-core install chromium` 한 번이면 된다.
**경로를 검사 파일에 다시 박지 말 것.** 세 곳에 흩어져 있던 탓에 한 곳만
고쳐지고 나머지가 조용히 죽은 적이 있다.

CI(`.github/workflows/android.yml`)는 `npm test`가 통과해야 APK를 만든다.
검사를 빌드 뒤에 두면 깨진 빌드가 먼저 릴리스에 올라간다.

## 데이터

`localStorage["jaksim3.v1"]` 한 곳에 전부 들어간다. 서버도 계정도 없다.

```js
goal = {
  id, title, icon, createdAt,
  checks: [],          // 이번 사이클에 체크한 날 (최대 3)
  history: [],         // 지금껏 체크한 모든 날 — 기록 화면의 재료
  lastCheckDate: null, // 같은 날 두 번 세지 않으려고
  totalDays, completedCycles, restarts,
  stoneShapes: [],     // 예전에 고르던 돌 모양 — 지금은 그림에 쓰지 않는다
}
```

`restarts`는 **다시 쌓기를 시작한 횟수**다. 끊긴 뒤(broken)와 3일을 채우고
오래 쉰 뒤(lapsed) 둘 다 센다. 완주 다음 날 바로 이어 가는 것(resting)은
멈춘 적이 없으므로 세지 않는다. 이 수는 공유 카드로 남의 담벼락까지
나가므로, 표시 문구도 '무너졌다'가 아니라 '멈췄다가'로 맞춰 두었다.

작심은 **`GARDEN_MAX`(6)개까지** 만들 수 있다. 정원에 놓인 줄이 여섯이라
그 이상은 그림에 설 자리가 없다. 새로 만드는 것만 막고, 예전에 만들었거나
가져온 여섯 초과 기록은 지우지도 숨기지도 않는다.

**한 화면은 유한하지만 빠지는 탑은 없다.** 정원 탭은 `towersPerPage()`가 정한
만큼만 세우고 나머지는 **쪽을 넘겨서** 본다(옆으로 밀거나 ‹ ›). 10년을 다닌
사람의 탑 1458채를 한 번에 그리면 SVG 요소가 4만 개가 되고, 그러고도 탑
하나가 1px이 안 돼 아무것도 보이지 않는다. 한계는 화면 전체가 아니라 **한
줄**에 있다 — 한 작심의 탑이 열 채를 넘으면 좌우로 흔들어도 밧줄처럼 땋인다.

`해낸 날`·`쌓은 돌`·정원 문구의 `탑 N채`는 늘 전체를 말한다. 그림이 줄어드는
것과 기록이 줄어드는 것은 다르고, 그 구분이 흐려지면 이 탭을 만든 이유가
없어진다.

**필드를 더할 때는 `load()`에 예전 데이터를 이어받는 줄을 함께 넣는다.**
이미 쓰고 있는 사람의 기록이 깨지면 그걸로 끝이다.

상태는 `goalStatus()`가 fresh / active / complete / resting / lapsed /
broken 여섯 중 하나로 답한다. 완주를 날짜로 갈라 두는 이유가 주석에
적혀 있으니 손대기 전에 읽을 것.

## 지켜야 할 것

**날짜는 `now()`와 `todayStr()`만 지난다.** `new Date()`를 직접 부르면
개발자 도구의 '날짜 밀기'가 통하지 않아 3일치 흐름을 손으로 사흘
기다려야 검사할 수 있다.

**화면을 새로 만들면 `test/contrast.js`의 `screens`에 등록한다.**
밝은/어두운 조합 4가지를 돌며 모든 글자의 명암비를 잰다(WCAG AA).
기기 설정과 앱 설정이 엇갈린 조합에서 흰 글자가 사라진 적이 있어,
눈으로 보는 것으로는 대신할 수 없다.

**같은 것을 정하는 화면을 두 벌 만들지 않는다.** 만들기 시트와 고치기
시트가 그래서 한 벌을 나눠 쓴다. 두 벌이면 반드시 한쪽만 고쳐진다.

## 주석과 커밋

주석과 커밋 메시지 모두 **한국어**로, **무엇을 했는지가 아니라 왜
그랬는지**를 적는다. 코드를 보면 무엇인지는 알 수 있지만, 왜 그렇게
했는지는 적어 두지 않으면 사라진다.

커밋 제목은 무엇이 달라졌는지 한 줄로 (`돌탑을 다시 그렸다 — 조약돌
윤곽, 맞물린 원근`). 본문은 **어떤 문제가 있었고 왜 이렇게 풀었는지**를
문단으로 쓴다. 변경 목록을 불릿으로 나열하는 것은 이 저장소의 방식이
아니다 — `git log`를 몇 개 읽어 보고 그 결을 따를 것.

## 브랜치

작업 브랜치는 `claude/jaksim-samil-app-salp5o`.
푸시는 `git push -u origin claude/jaksim-samil-app-salp5o`.
PR은 요청받았을 때만 만든다.
