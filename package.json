{
  "name": "jaksimsamil",
  "version": "1.0.0",
  "private": true,
  "description": "작심삼일 — 딱 3일만 약속하는 습관 앱",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "sync": "npm run build && cap sync",
    "ios": "npm run sync && cap open ios",
    "android": "npm run sync && cap open android",
    "test": "node test/smoke.js && node test/pwa.js && node test/contrast.js",
    "live": "node scripts/live-config.js",
    "toss:build": "npm run build && ait build",
    "toss:deploy": "ait deploy"
  },
  "dependencies": {
    "@capacitor-firebase/analytics": "^8.3.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/local-notifications": "^8.2.1",
    "@capacitor/share": "^8.0.1",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3",
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "@apps-in-toss/web-framework": "^3.2.0",
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "playwright-core": "^1.62.1"
  }
}
